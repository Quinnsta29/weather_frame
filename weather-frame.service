[Unit]
Description=Weather Frame Service
After=network.target

[Service]
User=pi
WorkingDirectory=/home/pi/weather-frame/src
ExecStart=/usr/bin/python3 -m weather_frame.main
Restart=always
RestartSec=10
Environment="PYTHONPATH=/home/pi/weather-frame/src"
Environment="DEBUG_MODE=0"

[Install]
WantedBy=multi-user.target

# sudo nano /etc/systemd/system/weather-frame.service

# sudo systemctl daemon-reload
# sudo systemctl enable weather-frame.service
# sudo systemctl start weather-frame.service